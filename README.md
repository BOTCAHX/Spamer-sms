# Spamer-sms


# Install
```
$ cd
$ pkg update -y && pkg upgrade -y
$ pkg install git -y
$ pkg install python -y
$ pip install requests
$ git clone 
```
# Eksekusi
```
$ cd Spam_sms
$ python run.py
```
